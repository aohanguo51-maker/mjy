# 毛记忆 PawMemory —— 后端部署操作手册

> 环境 ID：`pawmemory-d5gjmq8i444ffb334`（上海，体验版，6个月免费）
> 全程在**你自己的电脑终端**操作，密钥不经过我这边。

---

## 开始前确认

你之前已经装好并登录了，先验证还有效：

```bash
tcb -v
tcb login --check
```

看到版本号和"登录成功"就继续。如果提示未登录，重新跑 `tcb login` 扫码。

---

## 第 1 步：把代码下载到本地

```bash
curl -o ~/Desktop/pawmemory.tar.gz http://10.4.130.34:8899/pawmemory-cloud.tar.gz
cd ~/Desktop && tar -xzf pawmemory.tar.gz && cd pawmemory-cloud
ls
```

应该看到：`functions`、`scripts`、`static`、`cloudbaserc.json`、`README.md`

> 如果 curl 连不上（超时/拒绝），告诉我，我换个方式给你代码。

---

## 第 2 步：设置登录密钥

这一步是为了让登录凭证不能被伪造。**8 个函数必须用同一个密钥**。

先生成一个随机密钥：

```bash
openssl rand -hex 32
```

复制输出的那串字符（64位十六进制），然后：

```bash
SECRET=粘贴刚才那串
for fn in auth user pet memory post interact medical bill; do
  tcb fn config update $fn --envId pawmemory-d5gjmq8i444ffb334 --envVariables TOKEN_SECRET=$SECRET
done
```

> 注意：这一步要在**函数部署之后**才能生效，所以如果报"函数不存在"，先跳过，等第 4 步做完再回来执行。

---

## 第 3 步：创建数据集合

```bash
bash scripts/init-db.sh
```

会依次创建 10 个集合（users / pets / memories / posts / comments / likes / follows / medical / bills / sms_codes）。
提示"已存在"是正常的，不影响。

验证：

```bash
tcb db:listCollections --envId pawmemory-d5gjmq8i444ffb334
```

> 这一步走 `tcb login` 的登录态，**不需要密钥**。

---

## 第 4 步：部署云函数

先给每个函数装依赖（一共 9 个，会花几分钟）：

```bash
for fn in auth user pet memory post interact medical bill audit seed; do
  echo "=== 安装 $fn ==="
  (cd functions/$fn && npm install --production)
done
```

然后一次性全部部署：

```bash
tcb fn deploy --all --envId pawmemory-d5gjmq8i444ffb334 --force
```

看到 9 个函数都显示部署成功即可。

**做完这步，回头执行第 2 步的密钥设置。**

---

## 第 4.5 步：写入官方示例内容

函数部署完之后执行：

```bash
tcb fn invoke seed --envId pawmemory-d5gjmq8i444ffb334
```

看到返回 `posts: 8` 之类就成功了。
这个命令**可以重复执行**，第二次会自动跳过，不会产生重复数据。

---

## 第 5 步：部署前端页面

```bash
tcb hosting deploy static/ / --envId pawmemory-d5gjmq8i444ffb334
```

---

## 第 6 步：拿到访问地址

```bash
tcb hosting detail --envId pawmemory-d5gjmq8i444ffb334
```

输出里的 **默认域名** 就是你的网址，形如：
`https://pawmemory-d5gjmq8i444ffb334-1234567890.tcloudbaseapp.com`

**这个域名不用备案，可以直接发给养宠人试用。**

---

## 第 7 步：验证

用手机或浏览器打开上面的网址：

1. 应该看到登录页
2. 输入任意手机号（如 13800138000）
3. 点"获取验证码"
4. 验证码填 `123456`
5. 登录进去，能看到官方示例动态
6. **关键测试**：改个昵称 → 关掉浏览器 → 重新打开 → 昵称还在

第 6 条通过，说明数据真的存到云端了。

---

## 常见问题

**Q：部署函数报 "function not found"**
先确认在 `pawmemory-cloud` 目录下，且 `cloudbaserc.json` 存在。

**Q：npm install 很慢**
换国内源：`npm config set registry https://registry.npmmirror.com`

**Q：登录后一直转圈 / 报错**
八成是 TOKEN_SECRET 没设置或不一致。检查：
```bash
tcb fn detail auth --envId pawmemory-d5gjmq8i444ffb334
```

**Q：登录后马上又跳回登录页**
同上，密钥不一致导致凭证校验失败。

**Q：图片显示不出来**
云存储权限问题。控制台 → 云存储 → 权限设置 → 选「所有用户可读，仅创建者可写」

---

## 还没做的（上线前必须）

**内容安全审核**：用户发的动态和评论要过机审，这是 UGC 合规底线。
代码骨架已经写好（`functions/audit`），但需要你开通服务：
1. 去 https://console.cloud.tencent.com/cms 开通「文本内容安全」和「图片内容安全」
2. 创建子账号密钥（只给 TMS + IMS 权限）
3. 配到 audit 函数的环境变量：`CMS_SECRET_ID` / `CMS_SECRET_KEY`

**真实短信验证码**：现在是固定 `123456`，页面上有标注。
要接真实短信需要申请短信签名和模板（1-3个工作日审核），之后改 `functions/auth/index.js` 里发码那一段。

---

## 到期提醒

体验版 **2027-01-29 左右到期**。到期前记得续费或迁移，否则数据可能被清理。
